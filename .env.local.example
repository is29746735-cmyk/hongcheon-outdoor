# 네이버 클라우드 플랫폼(NCP) > Maps 에서 발급받은 Client ID
# 이 파일을 .env.local 로 복사한 뒤 값을 채우세요.
NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=

# 날씨 정보는 Open-Meteo(무료, 키·회원가입 불필요)를 사용합니다.
# 별도 환경변수가 필요 없습니다. (/api/river-status 가 자동으로 호출)
